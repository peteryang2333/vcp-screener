# VCP 扫描器修复说明（2026-07-30）

## 问题：VCP 标的没出现在「日报」里
用户本地跑出 41 只 VCP 标的，但每日推送里没有。

## 根因（三层）
1. **两条独立系统，原本就不交叉**
   - `daily_signal` 自动化（本任务）：只推送**杠杆 ETF 轮动**（FAS/ERX），从未包含 VCP。
   - VCP 扫描：独立项目 `/Users/a1/Claude/VCP/`，由**单独的 crontab + 守护任务**生成 `信号早报`，与本推送无关。
2. **VCP 自动化扫描连挂 ~5 天返回 0**
   - crontab 里 VCP 条目被**重复写了 12 条** → 同一时刻 12 个进程并发打 Yahoo → 触发匿名 429 限流。
   - `yfinance 1.2.0` 把多标的下载结果改成 `(Price, Ticker)` 二级列，`extract_ticker_df` 仍按旧 `(Ticker, Price)` 取数 → **静默解析失败 → 0 候选**（不报错，最隐蔽）。
3. **残留：Yahoo 匿名 429 限流窗口**
   - 修复后重跑全量 903 只仍被限流（SPY 6月涨幅显示 0.0% 即为 SPY 也被限流）。
   - 已离线验证解析与 SPY 涨幅计算逻辑**完全正确**，0 结果仅由限流造成；用户本地跑通是因为命中了限流窗口冷却期 / 不同网络。

## 已修复
- ✅ crontab 去重：12 条 → 3 条（US 05:30 / JP 15:00 / KR 15:30，均为收盘后）。
- ✅ `extract_ticker_df`：兼容 yfinance 1.x 的 `(Price, Ticker)` 与旧 `(Ticker, Price)` 两种 MultiIndex 层级。
- ✅ `get_spy_performance`：修正 SPY 基准的 MultiIndex 取值。
- ✅ `safe_download`：指数退避 + 随机抖动 + 基础延时 2.0→3.5s；保留 yfinance 自带 curl_cffi 会话（1.2.0 拒绝外部 requests.Session）。
- ✅ 离线单测通过：MultiIndex 解析 OK、SPY 涨幅 +10.0% 正确。
- ✅ 已把用户本地 07-30 的 41 只写入 `数据/最新_US.txt`，供每日推送读取（待自动化扫描自愈合后会被真实扫描覆盖）。

## 本推送新增
- daily_signal 自动化第 4 步：读取 `数据/最新_US.txt`，在每日推送中附「VCP 美股精选前 10（距突破最近）」。
- GitHub 核对步骤改为 `curl + Keychain`（MCP 连接器当前 disconnected）。

## 待观察 / 后续
- 全量 903 只扫描仍可能在 Yahoo 限流热窗口失败；建议日常仅靠一次/日定时运行（已降并发），避免手工连跑触发限流。
- 若需稳定出数，可考虑：① 换用带认证的 Yahoo 会话；② 扫描改在本地缓存 CSV 上增量更新；③ 缩小宇宙（如仅 SP500）。
- 用户本地表含 `ATR止损` 列，而 `/Users/a1/Claude/VCP/vcp_screener.py` 用 `硬止损价` —— 用户本地是**不同/更新版本**，注意列名差异。

## 方案 B 完成（18:40）—— 本地数据同步上云 + 解除分叉
- 用户选 B：用非破坏方式把本地可跑代码 + 41 只 7/30 数据推上 GitHub，不硬推、不覆盖远程 18 个 commit。
- 操作：先 `rsync` 全量备份本地仓库到 `VCP_backup_20260730_184003` 保底 → `git reset --hard origin/main` 把工作树对齐到远程干净状态 → 仅从备份恢复 `vcp_screener.py` 与 41 只数据文件（`数据/最新_US.txt` + `数据/vcp_US/JP/KR_20260730.txt`）→ 只 `git add` 这 6 个文件后单提交。
- 关键：`vcp_screener.py` / `最新_JP.txt` / `最新_KR.txt` 与远程已完全一致（方案 A 已通过 API 推送修复版），故本次实际只新增/更新了 7/30 数据；`git push` 为 fast-forward（`83357c7..8c9d4a2`），**零远程 commit 被覆盖**。
- 4 个本地独有 commit（073d503/40468b6/1e3c2b0/8799f43）是已被远程重构取代的陈旧 workflow 配置（远程甚至删了 `daily-signal.yml`），价值全在本地工作树未提交的精细化修复与数据快照，已干净合入、未把这些陈旧 commit 拖进去。
- 验证：`git rev-list --left-right --count HEAD...origin/main` = `0 0`（分叉解除）；远程 `数据/最新_US.txt` 现含完整 41 只（markdown 表格 49 行）；`vcp-us-daily.yml` 仍在；`vcp_screener.py` 远程含 3 处修复命中。
- 日常：workflow `vcp-us-daily.yml` 每日 21:30 UTC 自动扫描并 commit `数据/`，会自然覆盖 `最新_US.txt`；本地 crontab 已去重为 3 条，无需重复维护。

## 方案 C 完成（2026-07-31 13:xx）—— 本地价缓存离线扫描（彻底绕开 Yahoo 限流）

问题仍未根治：7/30 的代码修复（MultiIndex + 退避 + 去重）虽已推上 GitHub（commit 8c9d4a2，与 origin/main 0/0 同步），但 **Yahoo 匿名限流（429）持续存在**，7/31 自动扫描 `数据/vcp_US_20260731.txt` 仍 0 命中、64 行限流报错，导致早报 VCP 段连续多日显示"零信号"。

根因定位：早报 VCP 段读取的 `results/最新.txt`（任务说明 step1.1）**从未被 workflow 更新**——`vcp-us-daily.yml` 只 commit `数据/`，不碰 `results/`。旧 `results/最新.txt` 是 7/29 的 SP500-only 旧格式，早已 stale。

修复动作（全在本地，未破坏远程）：
- 新增 `build_vcp_cache.py`：用 Yahoo chart API（`range=1y&interval=1d`）**单只拉取日线**，带 429 退避，产出 `数据/vcp_cache.pkl`（903 只 SP500+SP400 + SPY，日线 ~252 根）。此前 `.wb_fetch_prices.py` 漏写 range/interval 参数，误抓成"单日 1 分钟"数据，无法驱动 VCP（200 日 SMA 全 NaN）——此坑已避开。
- 改 `vcp_screener.py`：`scan_vcp` 与 `get_spy_performance` 在 `CACHE_PATH` 存在时**完全离线取数**，不再触碰 Yahoo；修掉原"缓存分支"的死代码 bug（`raw is None → continue` 会跳过整批，使缓存在限流场景下永远到不了）。
- 新增 `gen_latest_us.py` / `gen_results_latest.py`：用缓存跑扫描，生成 `数据/最新_US.txt`（markdown 表）与 `results/最新.txt`（早报读取源）。
- 7/31 缓存模式实跑：**命中 58 只 VCP 形态标的**（SP500+SP400），早报 VCP 段已改为真实清单（VOYA/RL/UAL/ARWR 等前排），并重新推送。

注意 / 后续：
- ~~`数据/vcp_cache.pkl` 是**本地缓存，不进 git**~~ —— **已改为进 git**（见下）。
- 本地 crontab 21:30 后跑扫描时会命中缓存→出真实信号。

## 7/31 后续：缓存纳入 git，workflow 离线扫描（用户要求）
- `数据/vcp_cache.pkl` **现已提交到 git**（934 只：SP900+SPY+ADR 日线），workflow 检出即可离线扫描，不再依赖 Yahoo。
- `build_vcp_cache.py` 改造为**增量/新鲜度策略**：默认仅补抓「缓存缺失」标的（如新加 ADR）；缓存 >7 天或 `--force` 才全量刷新；几乎全失败时保留旧缓存不覆盖。路径改为相对脚本目录，CI/本机通用。
- 新增 `数据/adr_list.txt`（中概/外企 ADR 清单），`vcp_screener.py` 新增 `--pool adr` 池，可单独扫描 ADR。
- `vcp_screener.py`：检测到 `CACHE_PATH` 存在时**跳过联网检测**，纯离线扫描（避免 Yahoo 全挂时误退出）。
- `vcp-us-daily.yml` 改为：检出 → 刷新缓存 → 跑 SP900 扫描 + 跑 ADR 扫描 → 提交 `数据/`（含 pkl，仅在刷新时变动，避免每日膨胀）。
- 体积极限：pkl 约 11MB，仅全量刷新周提交一次；日常只提交小体积 txt。如需进一步瘦身可上 Git LFS（当前未用）。
